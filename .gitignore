# ── Gradle / Java (mod/) ───────────────────────────────────────────────
.gradle/
**/build/
!gradle/wrapper/gradle-wrapper.jar
**/run/
.kotlin

# ── Rust / Tauri (app-desktop/) ────────────────────────────────────────
**/target/
app-desktop/gen/

# Mojang-derived item/block textures (extracted locally by tools/extract-textures.ps1;
# not redistributed in-repo). Design art (brand/, banners/) IS committed.
client/assets/items/
client/assets/tex/

# ── Assembled bundle output (tools/package.ps1 copies client/ into these) ──
app-mobile/apps/com.mochi.piggleshop/shop/
app-mobile/apps/com.mochi.piggleshop/assets/
app-mobile/apps/com.mochi.piggleshop/piggle-sdk.js
app-desktop/web/shop/
app-desktop/web/assets/
app-desktop/web/piggle-sdk.js

# ── Node (if any tooling is added later) ───────────────────────────────
**/node_modules/

# ── Claude Code (agent worktrees, local settings) ──────────────────────
.claude/

# ── IDE / OS ───────────────────────────────────────────────────────────
.idea/
*.iml
*.iws
*.ipr
out/
.apt_generated
.classpath
.factorypath
.project
.settings
bin/
.vscode/
.DS_Store

# NOTE: mod/ compiles against the co-located MochiOS2.0 forge build output
# (deobf classes, compileOnly) for the connector API — see mod/build.gradle
# (mochi_forge_classes). Build MochiOS2.0/minecraft/forge first.

# AEM common API jar — third-party PROPRIETARY artifact, NOT redistributed here.
# Drop it into mod/libs/ locally (build.gradle resolves it; -Paem_jar=<path> also works).
mod/libs/

# Packaged bundle/release artifacts
dist/
